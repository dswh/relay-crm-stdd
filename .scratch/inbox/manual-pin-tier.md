# Let me manually mark a lead as hot

**From:** Marcus (Head of Sales) · **Status:** wontfix (rejected — out of scope)

I want to override a lead's tier by hand — pin one as "hot" regardless of its score —
because sometimes I just *know* a deal is live and the score hasn't caught up yet.

---

> *This was generated by AI during triage.*
>
> **Closed — wontfix (for now).** Manual tier override is explicitly out of scope in
> the Lead Scoring spec. Rationale recorded in `.out-of-scope/manual-tier-override.md`
> so we don't re-litigate it. Revisiting would need its own `/align` (how a pin
> interacts with decay and the digest).
