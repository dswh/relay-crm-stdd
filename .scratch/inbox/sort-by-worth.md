# Can we sort leads by how good they are, not just newest?

**From:** Dana (rep) · **Status:** wontfix (already implemented)

The leads list is ordered by recency, which means whoever poked us last is on top
even if they're a tyre-kicker. Can we rank by how promising a lead actually is?

---

> *This was generated by AI during triage.*
>
> **Closed — already implemented.** This is exactly **Lead Scoring**, which now
> ships: the list sorts by **score** (worth), not recency, with a tier badge per
> row. See `.scratch/lead-scoring/PRD.md` and `src/services/scoringService.ts`.
> Category `bug`-adjacent but really a `wontfix`/duplicate of shipped work — not
> recorded in `.out-of-scope/` because it *is* built, not rejected.
