---
Status: ready-for-agent
Type: enhancement
Mode: human-in-loop
---

> *This was generated by AI during triage.*

# 006 · weekly digest of hottest leads (per rep)

Promoted from `inbox/weekly-digest.md` (Sam, rep). Verified it's a genuinely new
capability — not the existing in-app sort.

## What to build

A Monday-morning email to each rep listing their top leads by score for the week
(e.g. their top 10 hot leads), so they start the week knowing who to call without
opening the app.

## Acceptance criteria

- [ ] A digest routine selects each owner's highest-scoring leads (default top 10).
- [ ] Output is per-rep (a rep sees only their own leads).
- [ ] Tiers/labels reused from scoring — no new "hotness" definition invented.
- [ ] Tested through the service seam on a seeded multi-rep repo.

## Context the agent needs

- Scoring + tiers live behind `scoringService` (`/deepen`). Reuse `getScore` and the
  tier bands; do **not** re-derive "hot".
- Email *delivery* is out of scope here — produce the digest payload; wiring a real
  mail provider is a separate, human-owned step (hence Mode: human-in-loop).

## Blocked by

- 002 (tiers) — the digest groups by hot/warm/cold.
