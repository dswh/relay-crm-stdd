# Relay CRM — Stage 09 complete: Inbox triaged

Relay is a tiny CRM we add a **Lead Scoring** feature to, one stage at a time. You
are on **`09_end`** — the finished version of stage 09 (`/triage`), a side-demo.

## What this stage produced
- One accepted, agent-ready issue: `.scratch/lead-scoring/006-weekly-digest.md`.
- A rejected request parked with a reason: `.out-of-scope/manual-tier-override.md`.
- The near-duplicate resolved against the existing plan; the inbox cleared.
- No application code changed — this stage sorts work, it doesn't build it.

## What to do next
- Try another side-demo (`08_begin`, `10_begin`, `11_begin`), or return to the finished feature with `git checkout 07_end`.

See `DEMO-BRANCHES.md` for the full map of every stage.
