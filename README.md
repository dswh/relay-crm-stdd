# Relay CRM — Stage 10 complete: Tiers built unattended

Relay is a tiny CRM we add a **Lead Scoring** feature to, one stage at a time. You
are on **`10_end`** — the finished version of stage 10 (`/afk`), a side-demo.

## What this stage produced
- Issue **002 (tiers)** built autonomously by one agent pass: hot / warm / cold logic in `src/services/scoringService.ts` (with tests) and a tier badge in the UI.
- `002-tiers.md` marked done.
- `npm test` →
  ```
   Test Files  2 passed (2)
        Tests  9 passed (9)
  ```

## What to do next
- Try another side-demo (`08_begin`, `09_begin`, `11_begin`), or return to the finished feature with `git checkout 07_end`.

See `DEMO-BRANCHES.md` for the full map of every stage.
