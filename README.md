# Relay CRM — Stage 00 complete: Repo is set up

Relay is a tiny CRM we add a **Lead Scoring** feature to, one stage at a time. You
are on **`00_end`** — the finished version of stage 00 (`/setup`).

## What this stage produced
- A new `docs/agents/` folder: `domain.md`, `issue-tracker.md`, `triage-labels.md`.
- An agent-skills configuration block added to `CLAUDE.md`.
- No application code changed. `npm test` → **3 tests pass**.

## What to do next
- The repo is now ready for the STDD skills. Start the design step:
  ```bash
  git checkout 01_begin
  ```

See `DEMO-BRANCHES.md` for the full map of every stage.
